#include <stdio.h>

/**
 * main - main function
 * @argc: numbers of stuff added
 * @argv: the things added
 * Return: always 0 SUCCESS
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	(void)argv;

	return (0);
}
