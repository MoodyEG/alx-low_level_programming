#include "main.h"
/**
 * main - check the code.
 * @c: our main int
 * 
 * Return: Returns 1 if c is uppercase, Returns 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
