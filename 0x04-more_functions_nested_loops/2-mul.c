#include "main.h"
/**
 * mul - multiply our numbers
 * @a: our main int
 * @b: our main int
 * 
 * Return: Returns 1 if c is uppercase, Returns 0 otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
